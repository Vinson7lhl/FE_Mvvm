export default () => {
	// console.log('router中间件')
	// console.log(app, route, redirect)
	// 获取当前router路径
	// const TARGET_URL = route.path
	// 遍历必要的页面看是否匹配，若匹配则判断是否有token
	// if (route.path.includes('/')) {
	// 	// 如果没有拿到token
	// 	redirect(`/login?fromUrl=${TARGET_URL}`)
	// }
}